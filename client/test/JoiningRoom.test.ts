import { MessageChannel } from "../src/Framework/MessageChannel";
import { JoinRoomViewModel } from "../src/App/ViewModel/JoinRoomViewModel";
import { Test } from "./TestRunner";

export class JoiningRoomTest {
    constructor() {

Test.case('JoningRoom: When I pass no Id, a new room should be created', () => {
    // Arrange
    const sut = new JoinRoomViewModel();
    
    // Act

    // Assert
});

    }
}